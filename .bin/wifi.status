#!/usr/bin/env dash

export MEMOIZE_CACHE="/tmp/${USER}-memoize"
if memoize 0 ps -u root -o command | grep -q '[/]usr/lib/iwd/iwd' ; then
  printf '%s\n' profile
  #printf '%s\n' enabled
else
  printf '%s\n' disabled
fi
