#!/usr/bin/env sh

profiles=$(wifi.profiles | cut -b3-)
profile=$(echo "$profiles" | dmenu_center --rest -i -t -sb '#D24858' -p "profile to watch" -l 10)

if [ "$profile" = '' ]; then
	exit 102
fi

terminator -b -x watch netctl status "$profile"
