#!/usr/bin/env sh
service="$(menu.systemctl.select --user)"
rc="$?"
if [ "$rc" -ne 0 ] ; then exit "$rc" ; fi

# shellcheck disable=SC1003
terminator -b -x watch systemctl --user status "$(systemd-escape "$(printf '%s' "$service" | escape-characters '\\' ' ')")" -n 25
