#!/usr/bin/env sh

if is.papercut.running
then
echo 'stop papercut'
ps -aux | grep papercut | grep java | awk '{print $2}' | xargs kill
else
echo 'papercut already stopped'
fi

if is.cups.running
then
echo 'stop cups'
systemctl stop cups-browsed.service
else
echo 'cups already stopped'
fi

