#!/usr/bin/env sh

SSIDs=$(wifi.fingerprint | jq -r '.[] | select( .ESSID != "" ) | .ESSID')
SSID=$(echo "$SSIDs" | dmenu_center --rest -z -p 'SSID')

if [ "$SSID" != '' ] ; then
	wifi.connect "$SSID"
fi
