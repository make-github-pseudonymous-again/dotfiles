#!/usr/bin/env sh

q=$(ui.dialog.input -p 'youtube')

if [ "$q" = '' ] ; then
	exit 1
fi

query=$(echo -n "$q" | urlencode)

firefox --new-window 'https://www.youtube.com/results?search_query='"$query"
